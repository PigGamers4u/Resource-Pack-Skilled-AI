# Schemas

Drop JSON Schema files here for editor validation/autocomplete (VS Code
`json.schemas` config) covering manifest.json, entity files, geometry,
animations, particles, etc. Community schema projects exist for Bedrock —
search for a current one that matches your target format_version rather
than assuming an old schema still lines up with newer fields.
